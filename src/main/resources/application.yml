
spring:
  h2:
    console:
      enabled: true # 웹 콘솔 옵션 활성화
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect # 출력되는 쿼리를 mysql버전으로 변경 (기본-hibernate)
    show_sql: true # 콘솔에서 쿼리 로그를 확인할 수 있음.